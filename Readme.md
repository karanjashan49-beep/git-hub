First install the package of git hub on the terminal.
create a directory of any name.
Go into that directory.
Commands execute in the repositry 
# git init
# git pull "clone code from your git-hub directory" = To pull everything in that directory
# git add "file name"
# git commit -m "v2"
# git remote add origin "clone code of repositry"
# git push -u origin main = To push file on git hub 
